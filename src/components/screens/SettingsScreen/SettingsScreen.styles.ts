import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  headerImage: {
    color: '#808080',
    bottom: -90,
    left: -35,
    position: 'absolute',
  },
  titleContainer: {
    display: 'flex',
    flexDirection: 'row',
    gap: 8,
  },
});

export {styles};
